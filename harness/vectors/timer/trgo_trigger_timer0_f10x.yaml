name: trgo_trigger_timer0_f10x
description: |
  Advanced-timer TIMER0 ADC-trigger config (M3 T6) on the F10x family: CH3 configured as the
  output-compare ADC-trigger channel (PWM mode 0 + compare shadow + the trigger compare value near
  the up-count top, ~CAR-1 = 2249) and the CR2/CTL1 master-mode TRGO selection = UPDATE event. This
  is the timer HALF of the timer->ADC injected coupling (the ADC half is inject_bringup_adc0); the
  cross-peripheral trigger-matrix invariant is asserted in test_timer.py against this + the ADC
  vector.

  CH3 is NOT a bridge phase output: its compare match is the injected ADC's external trigger. It is
  programmed as a compare channel (mode + shadow + CV) but its output (CH3EN) is left OFF, so this
  vector does NOT touch CHCTL2. The TRGO master mode is set to UPDATE (HP-5).

  runtime-hal: PwmTimer::at(TIMER0_BASE).configure_trigger(2249, Pwm0, false, Update). Programs CH3 PWM0 (CHCTL1[14:12]),
    CH3 compare shadow (CHCTL1 bit 11), CH3CV = 2249, and CTL1 MMC = UPDATE (2<<4).
  GD SPL: timer_channel_output_mode_config(TIMER0, TIMER_CH_3, TIMER_OC_MODE_PWM0);
          timer_channel_output_shadow_config(TIMER0, TIMER_CH_3, TIMER_OC_SHADOW_ENABLE);
          timer_channel_output_pulse_value_config(TIMER0, TIMER_CH_3, 2249U);
          timer_master_output_trigger_source_select(TIMER0, TIMER_TRI_OUT_SRC_UPDATE);

  The advanced-timer register block is IDENTICAL on both families (same offsets / bit positions).

# Mode rationale: final_state. API-FORCED CHANGE (post-refactor): PwmTimer::at went pub(crate), so
# both oracles now do the full advanced-timer bring-up (identical to pwm_bringup_timer0) AND THEN the
# four trigger calls. See trgo_trigger_timer0_f1x0 for the full rationale. Trigger end state:
#   CHCTL1 +0x1C : CH3 PWM0 | CH3 shadow ORed onto the bring-up CH2 config  0x00006868
#   CH3CV  +0x40 : trigger compare 2249                                     0x000008C9
#   CTL1   +0x04 : MMC = UPDATE ORed onto the bring-up idle 0x3F00          0x00003F20
mode: final_state

implementations:
  gd-spl/gd32f10x:
    includes: [gd32f10x.h, gd32f10x_timer.h, gd32f10x_rcu.h]
    body: |
      timer_parameter_struct tp;
      tp.prescaler        = 0U;
      tp.alignedmode      = TIMER_COUNTER_CENTER_UP;
      tp.counterdirection = TIMER_COUNTER_UP;
      tp.clockdivision    = TIMER_CKDIV_DIV2;
      tp.period           = 2250U;
      tp.repetitioncounter= 0U;
      timer_init(TIMER0, &tp);

      timer_oc_parameter_struct oc;
      oc.outputstate  = TIMER_CCX_ENABLE;
      oc.outputnstate = TIMER_CCXN_ENABLE;
      oc.ocpolarity   = TIMER_OC_POLARITY_HIGH;
      oc.ocnpolarity  = TIMER_OCN_POLARITY_LOW;
      oc.ocidlestate  = TIMER_OC_IDLE_STATE_HIGH;
      oc.ocnidlestate = TIMER_OCN_IDLE_STATE_HIGH;

      timer_channel_output_config(TIMER0, TIMER_CH_0, &oc);
      timer_channel_output_mode_config(TIMER0, TIMER_CH_0, TIMER_OC_MODE_PWM0);
      timer_channel_output_shadow_config(TIMER0, TIMER_CH_0, TIMER_OC_SHADOW_ENABLE);
      timer_channel_output_pulse_value_config(TIMER0, TIMER_CH_0, 0U);

      timer_channel_output_config(TIMER0, TIMER_CH_1, &oc);
      timer_channel_output_mode_config(TIMER0, TIMER_CH_1, TIMER_OC_MODE_PWM0);
      timer_channel_output_shadow_config(TIMER0, TIMER_CH_1, TIMER_OC_SHADOW_ENABLE);
      timer_channel_output_pulse_value_config(TIMER0, TIMER_CH_1, 0U);

      timer_channel_output_config(TIMER0, TIMER_CH_2, &oc);
      timer_channel_output_mode_config(TIMER0, TIMER_CH_2, TIMER_OC_MODE_PWM0);
      timer_channel_output_shadow_config(TIMER0, TIMER_CH_2, TIMER_OC_SHADOW_ENABLE);
      timer_channel_output_pulse_value_config(TIMER0, TIMER_CH_2, 0U);

      timer_auto_reload_shadow_enable(TIMER0);

      timer_break_parameter_struct bp;
      bp.runoffstate     = TIMER_ROS_STATE_ENABLE;
      bp.ideloffstate    = TIMER_IOS_STATE_ENABLE;
      bp.deadtime        = 0x1CU;
      bp.breakpolarity   = TIMER_BREAK_POLARITY_LOW;
      bp.outputautostate = TIMER_OUTAUTO_DISABLE;
      bp.protectmode     = TIMER_CCHP_PROT_OFF;
      bp.breakstate      = TIMER_BREAK_DISABLE;
      timer_break_config(TIMER0, &bp);

      /* T6 trigger step: CH3 compare channel + TRGO master-mode select. */
      timer_channel_output_mode_config(TIMER0, TIMER_CH_3, TIMER_OC_MODE_PWM0);
      timer_channel_output_shadow_config(TIMER0, TIMER_CH_3, TIMER_OC_SHADOW_ENABLE);
      timer_channel_output_pulse_value_config(TIMER0, TIMER_CH_3, 2249U);
      timer_master_output_trigger_source_select(TIMER0, TIMER_TRI_OUT_SRC_UPDATE);

  runtime-hal/gd32f10x:
    body: |
      use runtime_hal::{Chip, McuDescriptor, AddrTable, PeriphLabel};
      use runtime_hal::{AdcPath, ClockPath, GpioPath, IrqLayout, PageSize};
      use runtime_hal::timer::PwmTimer;
      use runtime_hal::config::{BreakConfig, PwmChannelConfig, PwmConfig, PwmAlign, OcMode, ClockDiv, TrgoSource};

      fn chip() -> Chip {
          let mut addrs = AddrTable::new();
          addrs.set(PeriphLabel::Timer0, 0x4001_2C00);
          Chip::from_descriptor(McuDescriptor {
              gpio: GpioPath::ApbCrlCrh, clock: ClockPath::F10xRcc, adc: AdcPath::Single,
              irq: IrqLayout::F10xSeparate, addrs, flash_page: PageSize::K2, flash_kib: 256, adv_timers: 1, adc_count: 1,
          })
      }

      pub fn body() {
          let ch = |high: u8, low: u8| PwmChannelConfig { high, low, polarity: true, idle_high: true, idle_high_n: true };
          let cfg = PwmConfig {
              timer: PeriphLabel::Timer0,
              channels: [ch(0x08, 0x1D), ch(0x09, 0x1E), ch(0x0A, 0x1F)],
              period: 2250, prescaler: 0, dead_time: 0x1C,
              brk: BreakConfig { enabled: false, level: false },
              trigger_compare: 2249,
              align: PwmAlign::Center2, arse: true, trigger_oc_mode: OcMode::Pwm0,
              trigger_ch_enable: false, crep: 0, ckdiv: ClockDiv::Div2, trgo_src: TrgoSource::Update,
          };
          let t = PwmTimer::configure(&chip(), &cfg).unwrap();
          t.configure_trigger(2249, OcMode::Pwm0, false, TrgoSource::Update);
      }
